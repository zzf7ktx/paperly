PAPERLY DESKTOP

Build the Windows installer:
  npm run build:electron

Run the desktop app during development:
  npm run electron

The packaged application runs Paperly locally. PDF files and edits are not
uploaded, and the editor window has no Node.js access.
